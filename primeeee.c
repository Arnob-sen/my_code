#include <stdio.h>
#include <math.h.
void main()
{
    int n,i,p;
    printf("enter the value :");
    scanf("%d",&n);
    p=sqrt(n);
    for (i=2;i<=sqrt(n);i++){
        if(n%i==0){
            break;
        }
        else if(n%i!=0){

        }
    }

}
